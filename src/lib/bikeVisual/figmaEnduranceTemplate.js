export const FIGMA_ENDURANCE_TEMPLATE = {
  source: "Figma / FrameTemplate_Endurance｜耐力架 / 1:6",
  templateNodeId: "1:6",
  frameNodeId: "1:823",
  anchorsNodeId: "1:846",
  canvas: { width: 1920, height: 1080 },
  anchors: {
    bottomBracket: { x: 880, y: 788 },
    rearAxle: { x: 578, y: 732 },
    frontAxle: { x: 1328, y: 732 },
    seatTubeTop: { x: 786, y: 467 },
    seatpostSocketAnchor: { x: 786, y: 467 },
    topTubeSeatJoint: { x: 786, y: 467 },
    topTubeHeadJoint: { x: 1176, y: 394 },
    downTubeHeadJoint: { x: 1189, y: 435 },
    seatStayHighConnection: { x: 792, y: 488 },
    seatStayMidConnection: { x: 806, y: 539 },
    seatStayLowConnection: { x: 820, y: 590 },
    headTubeBottom: { x: 1201, y: 461 },
    headTubeTop: { x: 1176, y: 376 },
    saddleAnchor: { x: 706, y: 262 },
    handlebarAnchor: { x: 1348, y: 319 },
    spacerToHeadTubeJoint: { x: 1167, y: 341 },
    stemToSpacerJoint: { x: 1167, y: 341 },
    stemToHandlebarJoint: { x: 1259, y: 329 },
    drivePedal: { x: 1004, y: 788 },
    nonDrivePedal: { x: 756, y: 788 },
  },
  anchorNodes: {
    BB_Center: { nodeId: "1:854", point: { x: 880, y: 788 } },
    RearAxle: { nodeId: "1:853", point: { x: 578, y: 732 } },
    FrontAxle: { nodeId: "1:852", point: { x: 1328, y: 732 } },
    HeadTop: { nodeId: "1:850", point: { x: 1176, y: 376 } },
    HeadBottom: { nodeId: "1:849", point: { x: 1201, y: 461 } },
    SeatCluster: { nodeId: "1:851", point: { x: 786, y: 467 } },
    TopTubeSeatJoint: { nodeId: "80:5942", point: { x: 786, y: 467 } },
    TopTubeHeadJoint: { nodeId: "80:5944", point: { x: 1176, y: 394 } },
    DownTubeHeadJoint: { nodeId: "80:5943", point: { x: 1189, y: 435 } },
    SeatStayJoint_Low: { nodeId: "51:2619", point: { x: 820, y: 590 } },
    SeatStayJoint_Mid: { nodeId: "51:2618", point: { x: 806, y: 539 } },
    SeatStayJoint_High: { nodeId: "51:2617", point: { x: 792, y: 488 } },
    SaddleAnchor: { nodeId: "1:848", point: { x: 706, y: 262 } },
    HoodContact: { nodeId: "1:847", point: { x: 1348, y: 319 } },
    StemToHandlebarJoint: { nodeId: "4:3794", point: { x: 1259, y: 329 } },
    StemToSpacerJoint: { nodeId: "4:3793", point: { x: 1167, y: 341 } },
    SpacerToHeadTubeJoint: { nodeId: "4:3786", point: { x: 1167, y: 341 } },
  },
  layers: {
    frame: { nodeId: "1:823", figmaName: "Frame｜车架", x: 553.297729, y: 361.15686, width: 668.237671, height: 447.52887 },
    cockpit: { nodeId: "1:845", figmaName: "Cockpit｜把组", x: 1148, y: 287, width: 237, height: 162 },
    frameDownTube: { nodeId: "2:895", figmaName: "DownTube｜下管", x: 858.616089, y: 407, width: 358.364258, height: 387.408783 },
    frameTopTube: { nodeId: "2:886", figmaName: "TopTube｜上管", x: 782.500025, y: 372.000023, width: 394.471497, height: 117.5 },
    frameHeadTube: { nodeId: "2:885", figmaName: "HeadTube｜头管", x: 1154.999756, y: 361.156433, width: 66.535416, height: 115.539024 },
    frameChainstay: { nodeId: "2:931", figmaName: "ChainStay｜后下叉", x: 559.297699, y: 708.00354, width: 291.347687, height: 93.99707 },
    frameSeatstay: { nodeId: "2:922", figmaName: "SeatStay｜后上叉", x: 566.910034, y: 488, width: 221.589966, height: 237.5 },
    frameSeatTube: { nodeId: "2:909", figmaName: "SeatTube｜座管", x: 559.297913, y: 361.157166, width: 662.237671, height: 447.528931 },
    frameBottomBracket: { nodeId: "2:902", figmaName: "BB_Shell｜五通", x: 824.999878, y: 719.834595, width: 113.089844, height: 88.851913 },
    frontRotor: { nodeId: "1:247", x: 1278, y: 682, width: 100, height: 100 },
    rearRotor: { nodeId: "1:256", x: 528, y: 682, width: 100, height: 100 },
    cassette: { nodeId: "1:79", x: 538, y: 692, width: 80, height: 80 },
    rearWheel: { nodeId: "1:217", x: 338, y: 492, width: 480, height: 480 },
    frontWheel: { nodeId: "1:223", x: 1088, y: 492, width: 480, height: 480 },
    seatpost: { nodeId: "1:298", figmaName: "Seatpost｜坐杆", x: 713, y: 272, width: 102, height: 256 },
    saddle: { nodeId: "1:300", x: 629, y: 242, width: 176, height: 40 },
    spacer: { nodeId: "1:282", figmaName: "垫圈", x: 1148, y: 313, width: 127, height: 106 },
    handlebarHood: { nodeId: "8:9679", x: 1240, y: 289, width: 136.383102, height: 127.999939 },
    handlebarTape: { nodeId: "8:9665", x: 1221, y: 287, width: 164, height: 162 },
    fork: { nodeId: "1:233", figmaName: "Fork｜前叉", x: 1190, y: 470, width: 160, height: 282 },
    chainring: { nodeId: "1:21", x: 805, y: 713, width: 150, height: 150 },
    driveCrank: { nodeId: "1:40", x: 864, y: 772, width: 156, height: 32 },
    nonDriveCrank: { nodeId: "1:41", x: 740, y: 772, width: 156, height: 32 },
    chain: { nodeId: "1:93", x: 567, y: 713, width: 313, height: 149.136 },
    derailleur: { nodeId: "1:88", x: 543, y: 746, width: 62, height: 110 },
  },
  assetAnchors: {
    // Midpoints of the two tangent points that define the seatpost SVG center axis.
    seatpostTop: { layer: "seatpost", x: 13.5, y: 12.5 },
    seatpostBottom: { layer: "seatpost", x: 86.5, y: 243.5 },
    // Named Cockpit connection anchors from Figma's shared Anchor layer.
    spacerHeadtubeAnchor: { layer: "spacer", nodeId: "4:3786", x: 19, y: 28 },
    // Midpoint of the distal edge of Spacer Vector 12; defines only its visual main axis.
    spacerVisualAxisEnd: { layer: "spacer", nodeId: "4:3789", x: 38.2318, y: 100.5043 },
    // Stem clamp location on the shared hood/tape assembly coordinate system.
    handlebarClampAnchor: { layer: "handlebarHood", x: 19, y: 39.79 },
    // Midpoint of the fork SVG top edge and center of its axle eye.
    forkTop: { layer: "fork", x: 16.16603, y: 4.0221845 },
    forkAxle: { layer: "fork", x: 138, y: 262 },
  },
};

export const ENDURANCE_SEAT_STAY_ANCHOR_KEYS = Object.freeze({
  low: "seatStayLowConnection",
  mid: "seatStayMidConnection",
  high: "seatStayHighConnection",
});

export function resolveEnduranceSeatStayAnchor(style) {
  const key = ENDURANCE_SEAT_STAY_ANCHOR_KEYS[style] ?? ENDURANCE_SEAT_STAY_ANCHOR_KEYS.mid;
  return { key, point: FIGMA_ENDURANCE_TEMPLATE.anchors[key] };
}

const difference = (end, start) => ({ x: end.x - start.x, y: end.y - start.y });

export function affineFromThreePoints(source, target) {
  const sourceX = difference(source[1], source[0]);
  const sourceY = difference(source[2], source[0]);
  const targetX = difference(target[1], target[0]);
  const targetY = difference(target[2], target[0]);
  const determinant = sourceX.x * sourceY.y - sourceY.x * sourceX.y;

  const a = (targetX.x * sourceY.y - targetY.x * sourceX.y) / determinant;
  const c = (-targetX.x * sourceY.x + targetY.x * sourceX.x) / determinant;
  const b = (targetX.y * sourceY.y - targetY.y * sourceX.y) / determinant;
  const d = (-targetX.y * sourceY.x + targetY.y * sourceX.x) / determinant;
  const e = target[0].x - a * source[0].x - c * source[0].y;
  const f = target[0].y - b * source[0].x - d * source[0].y;

  return { a, b, c, d, e, f };
}

export function similarityFromTwoPoints(sourceStart, sourceEnd, targetStart, targetEnd) {
  const source = difference(sourceEnd, sourceStart);
  const target = difference(targetEnd, targetStart);
  const denominator = source.x ** 2 + source.y ** 2 || 1;
  const a = (target.x * source.x + target.y * source.y) / denominator;
  const b = (target.y * source.x - target.x * source.y) / denominator;
  const c = -b;
  const d = a;
  const e = targetStart.x - a * sourceStart.x - c * sourceStart.y;
  const f = targetStart.y - b * sourceStart.x - d * sourceStart.y;
  return { a, b, c, d, e, f };
}

export function orientedSegmentTransform(sourceStart, sourceEnd, targetStart, targetEnd, lateralScale) {
  const source = difference(sourceEnd, sourceStart);
  const target = difference(targetEnd, targetStart);
  const sourceLength = Math.hypot(source.x, source.y) || 1;
  const targetLength = Math.hypot(target.x, target.y) || 1;
  const sourceAxis = { x: source.x / sourceLength, y: source.y / sourceLength };
  const sourceNormal = { x: -sourceAxis.y, y: sourceAxis.x };
  const targetAxis = { x: target.x / targetLength, y: target.y / targetLength };
  const targetNormal = { x: -targetAxis.y, y: targetAxis.x };
  const axialScale = targetLength / sourceLength;

  const a = axialScale * targetAxis.x * sourceAxis.x + lateralScale * targetNormal.x * sourceNormal.x;
  const c = axialScale * targetAxis.x * sourceAxis.y + lateralScale * targetNormal.x * sourceNormal.y;
  const b = axialScale * targetAxis.y * sourceAxis.x + lateralScale * targetNormal.y * sourceNormal.x;
  const d = axialScale * targetAxis.y * sourceAxis.y + lateralScale * targetNormal.y * sourceNormal.y;
  const e = targetStart.x - a * sourceStart.x - c * sourceStart.y;
  const f = targetStart.y - b * sourceStart.x - d * sourceStart.y;
  return { a, b, c, d, e, f };
}

export function resolveAssetAnchor(template, anchorName) {
  const anchor = template.assetAnchors[anchorName];
  const layer = template.layers[anchor.layer];
  return { x: layer.x + anchor.x, y: layer.y + anchor.y };
}

// The Figma canvas anchor is the real hood contact point, not a bounding-box
// estimate. Convert its local visual offset once for geometry consumers.
export function getHandlebarContactOffsetMm(referenceWheelDiameterMm = 686) {
  const clamp = resolveAssetAnchor(FIGMA_ENDURANCE_TEMPLATE, "handlebarClampAnchor");
  const hoodContact = FIGMA_ENDURANCE_TEMPLATE.anchors.handlebarAnchor;
  const mmPerFigmaPixel = referenceWheelDiameterMm / FIGMA_ENDURANCE_TEMPLATE.layers.rearWheel.width;
  return Object.freeze({
    x: (hoodContact.x - clamp.x) * mmPerFigmaPixel,
    y: (clamp.y - hoodContact.y) * mmPerFigmaPixel,
  });
}

export function uniformAroundPoint(source, target, scale) {
  return {
    a: scale,
    b: 0,
    c: 0,
    d: scale,
    e: target.x - source.x * scale,
    f: target.y - source.y * scale,
  };
}

export function applyMatrix(matrix, point) {
  return {
    x: matrix.a * point.x + matrix.c * point.y + matrix.e,
    y: matrix.b * point.x + matrix.d * point.y + matrix.f,
  };
}

export function composeMatrices(outer, inner) {
  return {
    a: outer.a * inner.a + outer.c * inner.b,
    b: outer.b * inner.a + outer.d * inner.b,
    c: outer.a * inner.c + outer.c * inner.d,
    d: outer.b * inner.c + outer.d * inner.d,
    e: outer.a * inner.e + outer.c * inner.f + outer.e,
    f: outer.b * inner.e + outer.d * inner.f + outer.f,
  };
}

export function matrixValue(matrix) {
  return `matrix(${matrix.a} ${matrix.b} ${matrix.c} ${matrix.d} ${matrix.e} ${matrix.f})`;
}
